
const u = require("wlj-utilities");

const generateGraph = require("../../library/generateGraph.js");
const index = require("../../index.js");

u.scope(__filename, x => {
    let graph = generateGraph(5);
    // TODO
    // u.assert(false);
});
